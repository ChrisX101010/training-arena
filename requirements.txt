# Core
pyyaml>=6.0
torch>=2.0
transformers>=4.35
datasets>=2.14

# PEFT/LoRA (fits training on 4GB VRAM)
peft>=0.10
trl>=0.8
bitsandbytes>=0.41

# Rating system
# pip install git+https://github.com/eth-sri/polyrating.git

# Wiki
gitpython>=3.1

# Dashboard
streamlit>=1.28
pandas>=2.0

# Ollama client
ollama>=0.1

# Optional: Axiom Wiki — npm install -g axiom-wiki
# Optional: LARQL — cargo install larql
